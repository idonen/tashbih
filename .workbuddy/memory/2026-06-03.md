# 2026-06-03 工作记录

## 创建 prayer.html 祈祷时间计算页面
- 根据 index.html 设计风格，基于 praytime.js API 实现完整的祈祷时间计算页面
- 功能：五次礼拜时间卡片、倒计时、地区搜索/GPS定位、计算方法选择、朝拜方向、日历条、周历表、多语言（zh/en/ar/ms）、深浅模式

## 优化：添加计算按钮 + 默认显示麦加时间
- 移除了四个 settings select 的自动 `onchange` 触发 updateAll 逻辑
- 改为修改参数后显示 pending-badge 提示徽章
- 新增绿色"计算祈祷时间"按钮（`doCalculate()`），点击后：
  1. 读取所有表单参数写入 state
  2. 调用 `updateAll()` 触发 praytime.js 重算
  3. 按钮显示旋转 → 对勾的视觉反馈
- 快捷城市/GPS 选择仍直接触发计算（用户明确意图）
- 页面 init 时默认麦加坐标 + `doCalculate()` 确保首屏直接显示结果
- 所有4种语言包新增 `btn_calculate` / `pending_changes` 词条
